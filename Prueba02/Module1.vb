Module Module1

    Sub Main() 'sub main -> nombreIdentificativo que tiene el procedimiento sub

        'intruccion de código
        Console.WriteLine("Hola Ceci")

        'instruccion, espera que el usuario ingrese una tecla x teclado y asi el programa se termine y cierre ok.
        Console.ReadKey(True)
    End Sub

End Module
